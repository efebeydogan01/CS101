import java.util.Scanner;

/**
 * A program to test Die class
 * @author Efe Beydoðan
 * @version 27.11.2019
 */ 
public class TestDie
{
   public static void main( String[] args)
   {
      Scanner scan = new Scanner( System.in);

      // constants

      // variables
      Die die1;
      Die die2;

      // program code
      die1 = new Die();
      die2 = new Die();
      
      System.out.println( "Face values of each die after the first roll: " + die1.getFaceValue() + " " + die2.getFaceValue() );
      
      die1.roll();
      die2.roll();
      
      System.out.println( "Face values of each die after the second roll shown by getFaceValue method: " + die1.getFaceValue() + " " + die2.getFaceValue() );

      System.out.println( "Face values of each die: " + die1 + " " + die2);
   }
}
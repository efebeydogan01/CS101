/**
 * A class that rolls one die
 */
public class Die
{
   // properties
   int face;
   
   // constructors
   public Die()
   {
      face = (int) (Math.random() * 6) + 1;
   }
   // methods
   
   /**
    * Rolls a die and returns its value 
    * @return current face value
    */
   public int roll()
   {
      face = (int) (Math.random() * 6) + 1;
      return face;
   }
   
   /**
    * Gives the current face value of the die
    * @return current face value
    */
    public int getFaceValue()
    {
       return face;
    }
    
     /**
    * Converts the face value to a string
    * @return current face value as string
    */
    public String toString()
    {
       return String.valueOf( face);
    }
}